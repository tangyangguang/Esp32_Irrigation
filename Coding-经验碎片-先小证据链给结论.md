# 经验碎片：先小证据链给结论，不盲目扩大范围

## 触发场景

用户问“现状是什么、方案是否 OK、需不需要重构、你怎么看”这类评估、咨询或点状反馈时，最容易误以为“查得越全越负责”。实际上，如果没有明确授权或会改变结论的新证据，跨文档、跨仓库、历史提交、全量契约和全量测试的大范围核查，会显著消耗时间、Token 和费用，却不增加结论价值。

## 教训

快速问题要先快速回答。默认动作应是读取直接责任文件和最小证据链，围绕用户关心的决策给出结论、依据和必要风险；只有出现以下情况，才扩大范围：

1. 已读证据互相矛盾，不扩大无法判断；
2. 涉及公共契约、跨仓生产者/消费者，局部信息可能误导；
3. 涉及安全、执行器、数据完整性或明确故障；
4. 用户明确要求全面审查、全量测试或跨仓核对。

没有这些触发条件时，不默认翻历史、不扫无关模块、不跑全量测试、不为“更放心”追加检查。

## 以后规则

- 先判断用户要的是“决策结论”还是“实施改动”：评估问题先回答，不把任务自动升级成审计。
- 第一轮只建立最小证据链：直接规则、直接实现、直接入口、关键测试或已有测试证据。
- 回答先给明确结论：保持/修改/重构/需要进一步确认，并说明依据。
- 发现局部小问题时，区分“影响当前结论的问题”和“可后续记录的问题”，不借题扩大需求。
- 需要扩大范围前，先说明触发原因和预计成本；能靠定向读取或定向测试解决，就不做全量核查。
- 测试服务于结论：没有代码改动、已有定向证据足够时，不为形式重跑；要跑也选择最小相关集合。
- 控制输出成本，不重复展示用户已知道的背景，不用大量过程材料淹没结论。

## 一句话

负责不是查得最多，而是用最小、足够的证据最快支持正确决策；扩大范围必须有明确触发条件和收益。
