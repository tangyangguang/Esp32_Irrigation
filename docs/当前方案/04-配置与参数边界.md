# 04 配置与参数边界

## 分层原则

配置只按作用域分层：

- 系统参数：影响全局。
- 水源配置：影响一个从站/水源。
- 水路配置：影响一条分支水路。

这是当前整理配置的起点，不是最终结论。如果后续发现有更合理的页面组织，可以调整。

不按“日常 / 高级 / 维护”分层。这个项目面向个人使用，只要参数真实影响行为，就应该分组清楚地展示。

## 系统参数

系统参数保留：

- 系统时间和 RTC 同步状态。
- 最大单次运行时长。
- 主阀和水泵启停延时默认值。
- 流量保护默认参数。
- RS485 通信速率。
- 从站离线判定参数。
- 通信异常判定参数。
- 输入映射：流量计使用 `IN1..IN4` 哪一路，低液位使用 `IN1..IN4` 哪一路。

默认值：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 最大单次运行时长 | 120 分钟 | 手动和计划都受限 |
| 主阀提前开启时间 | 1 秒 | 非负秒数 |
| 主阀延后关闭时间 | 1 秒 | 非负秒数 |
| 水泵/自吸泵启动延后时间 | 1 秒 | 非负秒数 |
| 水泵/自吸泵停止提前时间 | 1 秒 | 非负秒数 |
| 流量计输入 | IN1 | 可在 `IN1..IN4` 选择 |
| 低液位输入 | IN2 | 可在 `IN1..IN4` 选择，不能与流量计相同 |
| RS485 通信速率 | 待实现默认 | 方案只要求可配置 |
| 从站离线判定 | 待实现默认 | 方案只要求可配置 |

建议压缩表达：

- 页面只说明参数含义、单位、默认值和影响范围。
- 不展示底层调试细节。
- 修改系统参数只影响新任务或后续判断，不改写已经展开的任务。

## 水源配置

水源配置保留：

- 水源名称。
- 启用/停用。
- 从站地址显示。
- 主阀是否启用。
- 主阀使用阀号。
- 水泵/自吸泵是否启用。
- 流量计是否启用。
- 低液位是否启用。
- 低液位常开/常闭、去抖时间。
- 阀 PWM 参数。
- 电压读数显示。

默认值：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 水源启用 | 启用 | 停用只阻止新任务 |
| 主阀启用 | 不启用 | 启用后占用一个阀号 |
| 主阀阀号 | 阀 8 | 可改为阀 1..8 |
| 水泵/自吸泵启用 | 不启用 | 启用后使用从站干接点输出 |
| 流量计启用 | 启用 | 当前产品默认有流量计 |
| 低液位启用 | 不启用 | 低液位传感器不是必配 |
| 低液位触点 | 常开 | 可改常闭 |
| 低液位去抖 | 1 秒 | 实现阶段可调整默认 |
| 电压读数 | 只显示 | 不告警、不保护、不停机 |

边界：

- 从站地址来自拨码，Web 不修改地址。
- 电压只显示，不产生告警或保护。
- 输入只用于流量计和低液位，不做通用 IO。
- 流量计和低液位使用哪一路输入由系统参数统一配置；同一输入不能同时作为流量计和低液位。
- 干接点只作为水泵或自吸泵，不做通用继电器。

## 水路配置

水路配置保留：

- 水路名称。
- 启用/停用。
- 参考流量。
- 校准状态。

默认值：

| 参数 | 默认值 | 说明 |
| --- | --- | --- |
| 水路启用 | 启用 | 停用只阻止新任务 |
| 参考流量 | 未设置 | 校准或历史采用后生成 |
| 每升脉冲数 | 未设置 | 接水量校准后生成 |
| 最近手动时长 | 无 | 有历史后可复用 |

边界：

- 水路不直接选择阀号。
- 手动浇水时长不是水路配置。
- 计划浇水时长属于计划步骤。

## 运行中修改限制

建议规则：

- 水源运行中，不能修改该水源的主阀、水泵、流量计、低液位和 PWM 配置。
- 水源运行中，不能停用正在运行的水路。
- 停用水源或水路只阻止新任务，不停止当前任务。
- 停止任务必须使用明确的停止操作。

## 我的建议

- 系统参数页不应该变成“所有设置”。水源配置和水路配置必须回到对象详情里。
- PWM 可以展示，但必须收敛在水源配置里，不逐水路配置，也不放到运行页。
- 通信参数可以展示，但不要做成调试台。用户只需要知道默认值、可选范围和修改影响。
- 输入映射可以放在系统参数里，作为全局硬件映射。它不是通用 IO 配置，只是在 4 路输入中选择流量计和低液位各用哪一路。
- 配置页面要按用户理解组织，不强行证明只有三层。系统、水源、水路只是当前最自然的三个归属。

## 后续细化

后续只需要补默认值和可选范围：

- 水源级统一 PWM 参数。
- RS485 速率、轮询和离线判定参数。
- 流量保护参数的页面分组和说明文案。
