# ESP32 灌溉控制器固件

本目录是固件实现位置，代码只依据当前确认方案。

后续实现只依据：

- `../docs/当前方案/` 中确认的软件设计。
- `../pcb_irrigation/` 下的定稿 BOM 和网表。
- `../../Esp32Base` 已核实的公共能力。

当前已经实现配置安全保存、定稿 PCB 硬件层、浇水控制与保护、自动调度、流量计校准、水路流量学习、业务记录、应用事件、在线检查点和业务 Web 页面。标量系统参数统一使用 `Esp32Base` App Config，水路和计划使用列表加响应式弹层编辑。默认 Web 账号和密码均为 `admin`，用户可使用 `Esp32Base` 的 Web Auth 页面修改。

启动时只有加载到有效配置或成功创建默认配置后才继续；存在配置文件但所有副本均无效时保持输出关闭，不用默认值覆盖。RTC 倒退判断会参考最近浇水记录、应用事件和在线检查点。在线检查点仅在达到配置间隔且期间没有业务写入时保存，避免不必要的 Flash 磨损。

常用验证命令：

```sh
pio test -e native
pio run -e esp32_irrigation
pio test -e esp32_record_test --upload-port <serial-port> --test-port <serial-port>
```

基础库 Web OTA 已配置为设备 `192.168.2.136`。认证信息不得写入仓库；需要升级时由操作者在当前终端提供设备现行 Web Auth 凭据后，显式执行 `pio run -e esp32_irrigation -t webota`。普通构建和测试不会连接设备或触发 OTA。

```sh
export ESP32BASE_WEBOTA_USER='<current-web-auth-user>'
export ESP32BASE_WEBOTA_PASSWORD='<current-web-auth-password>'
pio run -e esp32_irrigation -t webota
```

2026-07-20 Web OTA 配置：复用 `../../Esp32Base/scripts/esp32base_webota.py` 和基础库 FULL profile 已启用的 Web OTA，目标 IP 配置为 `192.168.2.136`。执行 `pio test -e native` 通过 79/79，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 84028 B / 25.6%、Flash 1252969 B / 79.7%；按用户要求未执行 `webota`、串口上传或任何设备写入。

2026-07-14 的实际结果：本机 55 项测试通过，正式 ESP32 构建通过（RAM 78452 B / 23.9%，Flash 1137889 B / 72.3%），核心板上的 8 项既有测试通过。实机测试覆盖业务记录分页、详情、轮换、CRC 损坏识别、应用事件、调度状态 NVS 往返和损坏识别、在线检查点节流与重载。正式固件还完成了烧录、重启、默认认证、6 个页面、状态接口、CSV、跨站 POST 拒绝、配置版本冲突、计划增删、水路保存、自动暂停/恢复和手动启停回归；系统参数修改、确认、运行时生效与恢复正常，计划和水路弹层已通过桌面及手机宽度检查。本轮新增的稳态流量计校准、基准流量学习、水路表格、行动型首页、下一次自动浇水查询和暂停时间校验已通过 Native 回归、脚本语法检查与正式构建；System 页业务记录统一接入已通过静态契约检查与正式构建。浇水记录页的空状态、空数据分页抑制、深链接缺失提示和响应式布局已在烧录后的设备上通过 1280 px 与 390 px 浏览器检查；详情弹层代码已通过正式构建，仍需在设备产生真实浇水记录后补一次实机交互检查。

2026-07-15 的实际结果：执行 `pio test -e native` 通过 55/55 个测试用例，执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78452 B / 23.9%，Flash 1137889 B / 72.3%。

2026-07-15 浇水计划完整列表改造：执行 `pio test -e native` 通过 55/55 个测试用例，执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78452 B / 23.9%，Flash 1140117 B / 72.5%；通过 `/dev/cu.usbserial-57460296581` 烧录正式固件成功，写入 Hash 校验通过并正常硬复位。本轮按约定只完成代码级 HTML/CSS 检查，没有执行桌面或手机页面截图与人工视觉验收。

2026-07-15 浇水计划卡片列表优化：执行 `pio test -e native` 通过 55/55 个测试用例，执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78452 B / 23.9%，Flash 1140801 B / 72.5%；通过 `/dev/cu.usbserial-57460296581` 烧录正式固件成功，写入 Hash 校验通过并正常硬复位。本轮继续按约定不执行页面截图和人工视觉验收。

2026-07-15 浇水运行控制台改造：执行 `pio test -e native` 通过 57/57 个测试用例，新增覆盖运行来源、计划进度、剩余时间、实时估算水量、无基准流量采样、120 点流量环形缓冲和水路切换重置；嵌入式运行页脚本通过 Node.js 语法检查，执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78940 B / 24.1%、Flash 1158473 B / 73.7%。通过 `/dev/cu.usbserial-57460296581` 烧录正式固件成功，写入 Hash 校验通过并正常硬复位。烧录后本机无法解析 `irrigation-controller.local`，因此未完成设备 HTTP 接口复查；按约定未执行页面截图和人工视觉验收。

2026-07-15 首页与手动浇水信息架构重构：合并首页和浇水页，顶层业务导航收敛为首页、计划、水路、记录、设置；手动浇水支持选择计划模板填入后修改，计划页统一管理自动浇水暂停与恢复，水路页将流量计维护降为列表后的次要区域。执行 `pio test -e native` 通过 58/58 个测试用例，新增覆盖手动记录不关联计划；5 段完整嵌入式脚本通过 Node.js 语法检查，静态检查确认旧浇水页面和旧手动启动流程均已删除；执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78940 B / 24.1%、Flash 1151761 B / 73.2%。本轮未烧录设备，尚未完成 1280 px、390 px 页面视觉验收、真实运行态切换和受控供水出水验证。

2026-07-15 稳态流量系数、暂停交互与首页时间优化：用户界面统一使用“稳态流量系数（P/L）”，水路页移除参数跳转并压缩维护区；手动计划补充执行路数、总时长和逐区域预览；定时暂停统一提交最终恢复时间，小时数和明后天早上 6 点只作实时联动快捷输入；首页主状态区新增 UTC+8 秒级系统时间和 RTC/NTP 来源。执行 `pio test -e native` 通过 57/57 个测试用例，2 段完整嵌入式脚本通过 Node.js 语法解析，静态检查确认 `pulse/L`、`pause_hours` 和水路页“修改参数”入口均已删除；执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78940 B / 24.1%、Flash 1158625 B / 73.7%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，全部写入 Hash 校验通过；启动日志确认参数注册成功且业务进入 ready。实机桌面浏览器确认首页时钟连续走秒、系统参数名称与单位、手动计划逐区域预览、25 小时联动、明后天 6 点快捷项和水路维护布局均正常，页面无脚本错误；浏览器控制面未能切换到精确 390 px 视口，移动端媒体查询已完成代码级复查，仍需一次真实窄屏视觉确认。

2026-07-15 首页时间轻量化：移除时间卡片、标题和 UTC+8 标签，改为 24 px 深色秒级时间与 12 px 灰色自然日期/时间来源两行展示，移动端使用 14 px 纵向间距。执行 `pio test -e native` 通过 57/57 个测试用例，首页完整嵌入式脚本通过 Node.js 语法解析，执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 78940 B / 24.1%、Flash 1158537 B / 73.7%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验全部通过并正常硬复位；启动日志确认基础层完成启动、灌溉业务进入 ready、WiFi 连接、Web 服务启动且 NTP 成功同步。日志同时报告 DS3231 写回 `i2c_error`，不影响本次 NTP 时间与 Web 运行，但 RTC 硬件仍需在完整 PCB 上验证。本轮按约定未打开页面、未截图且未代替用户进行样式视觉验收。

2026-07-15 流量计校准闭环改造：校准采样从开阀开始执行 10 分钟硬上限，运行页补充最长时间和剩余上限；零脉冲及异常结束自动生成无效样本，正常非零采样支持保存水量或标记无效；有效和无效样本合计最多 10 条，支持修改有效样本水量、删除单条和全部清空；有效样本变化后自动拟合，并明确区分结果预览、设备当前参数、结果更新时间和本次应用时间。执行 `pio test -e native` 通过 62/62 个测试用例，新增覆盖零脉冲、异常样本、启动前失败、待填写处理、编辑删除、10 条总上限、应用元数据和从开阀计时的校准上限；校准运行脚本通过 Node.js 语法检查，静态检查确认旧 5 条上限已删除；执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 79084 B / 24.1%、Flash 1165917 B / 74.1%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验全部通过并正常硬复位；启动日志确认基础层完成启动、灌溉业务进入 ready、WiFi 连接、Web 服务启动且 NTP 成功同步。日志仍报告既有 DS3231 写回 `i2c_error`；本轮没有连接真实流量计和水路，也未打开页面或代替用户进行最终视觉验收。

2026-07-15 流量计校准多水路与拟合诊断改造：每个新样本均可自由选择任一已启用水路，样本保存并展示实际水路；带截距回归新增有符号启停非稳态净影响、等效水量、涉及水路数、逐样本预测脉冲和有符号残差，两样本明确标记为无法检验一致性。开始区改为紧凑横向操作栏，样本表合并运行信息并增加拟合偏差，结果区改为左右分栏统计布局且提供 900 px、760 px 和 480 px 响应式降级。执行 `pio test -e native` 通过 63/63 个测试用例，新增固定验收 `2217.21 P/L`、`-409.22` 脉冲、`-184.56 mL`、`0.09%` 最大残差及跨三条水路；执行 `pio run -e esp32_irrigation` 通过，资源占用为 RAM 79420 B / 24.2%、Flash 1170237 B / 74.4%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验全部通过并正常硬复位；启动日志确认基础层完成启动、灌溉业务进入 ready、WiFi 连接、Web 服务启动且 NTP 成功同步。日志仍报告既有 DS3231 写回 `i2c_error`；本轮未打开页面或代替用户进行最终样式视觉验收。

2026-07-15 流量计校准稳态阶段识别：新增按原始脉冲速率判断稳态的三个系统参数，检测窗口默认 3 秒、范围 1–10 秒，连续窗口默认 3 个、范围 2–10 个，允许波动默认 10%、范围 1%–30%。采样记录启动、稳态、停止三段时间与脉冲，未识别稳态自动保存为无效样本；拟合只使用稳态阶段脉冲和实测总水量，页面展示阶段数据、稳态速率、稳态/非稳态水量估算和残差。开始区、样本工具栏和结果区同步压缩布局，水路只显示名称。执行 `pio test -e native` 通过 65/65，其中控制器 23/23 并覆盖低脉冲速率下一个脉冲的窗口量化误差；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 80724 B / 24.6%、Flash 1175961 B / 74.8%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验通过；启动日志确认业务 ready、WiFi、Web 和 NTP 正常。校准页与状态接口实机 HTTP 均返回 200，页面路由包含新流程与配置说明。日志仍有既有 DS3231 写回 `i2c_error`；尚未连接真实流量计验证稳态识别，也未代替用户进行页面视觉验收。

2026-07-15 流量计校准代码与页面评审：修复新一轮采样可能复用上一轮稳态状态的问题，区分识别稳态平均速率与最近完整窗口速率，残差百分比改为相对模型预测值，并删除未使用的截距缓存和质量汇总接口。运行区用自然语言解释稳态确认进度和判定规则，收满窗口但波动不合格时明确提示继续滚动识别，并展示启动、稳态阶段的时间、脉冲与平均速率；停止后的三阶段摘要同步展示平均速率；样本表改为最多三列的响应式信息卡片，操作固定在卡片头部，避免横向滚动。项目仅按同级 `Esp32Base` 已发布接口适配离散事件写入、事件状态和就绪检查，没有引入持续条件事件逻辑。执行 `pio test -e native` 通过 66/66；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 80804 B / 24.7%、Flash 1183193 B / 75.2%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验通过；启动日志确认 App Events 存储与业务 ready，WiFi、Web 和 NTP 正常，校准页与状态接口实机 HTTP 均返回 200。日志仍有既有 DS3231 写回 `i2c_error`；未连接真实流量计复验稳态判断，也未代替用户进行页面视觉验收。

2026-07-15 流量计校准参数与紧凑页面：校准结果收敛为启动脉冲、估算启动水量、稳态流量系数和稳态参考流量四个可统一修改、持久回显的单值；当前只有稳态系数参与水量换算。页面顶部改为中性紧凑参数栏，停止摘要只展示启动和稳态，关闭后仍有脉冲时才提示尾水；样本改为共享列头的紧凑多行记录，规则说明只展示一次。执行 `pio test -e native` 通过 66/66，正式构建通过，资源占用 RAM 80892 B / 24.7%、Flash 1185997 B / 75.4%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验通过；启动日志确认通用配置字段全部注册、业务 ready、WiFi、Web 和 NTP 正常，校准页与状态接口实机 HTTP 均返回 200。新增的三个校准诊断参数由校准模块直接使用 NVS，不占基础库通用配置字段名额。仍需连接真实流量计验证四项建议值，并由用户完成最终页面视觉验收。

2026-07-15 业务事件与持续条件重构：顶层导航新增“事件”，页面以简洁业务语言提供最新优先列表、三级等级、业务分类筛选和详情，原始字段折叠为技术信息；App Events 只保留必要离散事实，并使用四个长期 `ConditionStateTracker` 跟踪硬件时钟不可用、设备时间不可用、时间倒退和关阀后水流异常。校准或水路学习未完成、记录 Store 初始化以及调度状态启动读取不再写事件；关阀后水流完成首个正常窗口前保持未知。执行 `pio test -e native` 通过 67/67，正式构建通过，资源占用 RAM 80980 B / 24.7%、Flash 1197957 B / 76.2%；ESP32 设备测试 8/8 通过，覆盖离散事件映射、维护操作抑制、条件发生/去重/恢复和业务文案。通过 `/dev/cu.usbserial-57460296581` 烧录正式固件，四个写入区 Hash 校验通过；启动测试发现并修复配置扫描与恢复写入共用深栈造成的首次启动栈溢出，修复后上电启动直接进入业务 ready，WiFi、Web 和 NTP 正常。业务事件页和原始事件 API 实机 HTTP 访问正常，重启后已活动条件没有重复写入；本轮未代替用户进行页面视觉验收，RTC 恢复和真实关阀后水流恢复仍需在完整硬件上验证。

2026-07-15 事件页面重新实现：筛选控件统一对齐，桌面端使用时间、等级、分类、事件、说明和操作六列表格，窄屏转换为结构化卡片；详情优先展示业务说明、事件概况和确定性的相关数据，技术字段默认折叠；事件时间统一为 `MM-DD HH:MM:SS` 且不换行，底部基础库入口恢复默认名称 `App Events`。执行 `pio test -e native` 通过 67/67，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 80980 B / 24.7%、Flash 1200841 B / 76.3%；通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验通过，启动日志确认业务 ready、WiFi、Web 和 NTP 正常，事件页实机 HTTP 与页面契约检查通过。日志仍有既有 DS3231 写回 `i2c_error`；页面最终视觉效果仍需用户在实机确认。

2026-07-16 页面样式修正：事件详情重置操作列继承的右对齐和禁止换行，事件名称恢复普通字重，业务数据按字段数量自适应铺满，技术信息降低视觉权重；流量计校准参数弹窗使用桌面两列、窄屏单列的专用字段布局。执行 `pio test -e native` 通过 67/67，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 80980 B / 24.7%、Flash 1201193 B / 76.4%；烧录后四个写入区 Hash 校验通过，启动日志确认业务 ready、WiFi、Web 和 NTP 正常，事件页与校准页实机 HTTP 布局契约检查通过。页面最终视觉效果仍需用户在实机确认。

2026-07-17 水路基准流量解耦：水路配置改为保存 `P/s × 100` 的原始基准脉冲速率，高低流量直接比较当前与基准原始速率，流量计校准系数只用于 `L/min` 显示和水量估算；学习继续使用最近 5 个 5 秒窗口，并新增每窗非零、10% 与单脉冲量化误差取大、总脉冲除以总时间生成结果、开阀后 10 分钟硬上限。水路页删除手工基准输入，改为学习、重新学习和清除；校准结果删除无控制用途的稳态参考流量。灌溉配置结构升级为版本 2，旧结构不迁移并保持安全停机，首页明确提示重新建立配置。执行 `pio test -e native` 通过 71/71 个测试用例，新增覆盖校准系数变化只改变显示、不改变报警判断，高低流量停止，20 秒偏差按窗口实际时长触发，单脉冲量化误差和零脉冲窗口；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 80996 B / 24.7%、Flash 1201405 B / 76.4%。本轮未烧录设备；仍需在真实水路验证稳定学习、低流量量化、系数变化后的显示以及高低流量触发。

2026-07-17 基准流量学习页面优化：将学习对象和运行状态合并为单一紧凑面板，页面正文取消粗体标签，实时流量、窗口平均和窗口换算流量统一显示为三位小数 `L/min`；稳定说明直接展示控制器使用的原始脉冲速率范围、速率跨度和允许跨度，最近 5 个窗口展示脉冲数、实际时长、`P/s` 与换算流量，并保留本次累计脉冲和当前流量计系数。状态接口同步提供最近 5 窗原始数据及稳定判定量，稳定阈值的状态展示与控制逻辑共用同一计算函数。执行 `pio test -e native` 通过 71/71 个测试用例，学习页嵌入脚本语法与页面字段契约检查通过，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 80996 B / 24.7%、Flash 1211121 B / 77.0%。本轮未烧录设备；页面最终样式和真实脉冲数据仍需在实机学习过程中确认。

2026-07-17 基准流量学习窗口展示优化：5 秒采样窗口继续互不重叠，稳定判断仍只使用最新 5 窗；状态改为保存并展示最近 10 个真实连续编号窗口，第 11 窗后显示范围由 `#1～#10` 滚动为 `#2～#11`。窗口表保持窗口、实际时长、脉冲数、脉冲速率、换算流量五列，脉冲数变化及换算流量变化值和百分比在对应主值右侧显示；最新 5 行使用轻量背景标识为当前判定范围。学习状态和结果补充累计窗口数、当前采集窗口、结果采用窗口及稳定跨度依据，学习过程累计脉冲明确包含建立水流阶段和已滚出窗口。执行 `pio test -e native` 通过 72/72 个测试用例，新增覆盖第 11 窗滚动、真实序号和最新 5 窗判定；学习页嵌入脚本语法与字段契约检查通过，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 81068 B / 24.7%、Flash 1213817 B / 77.2%。本轮未烧录设备；页面横向变化值布局和真实水路长时间滚动仍需实机确认。

2026-07-17 基准流量管理与校准参数页面闭环：已保存的水路基准同时展示三位小数 `L/min` 和实际保存语义对应的两位小数 `P/s`，不暴露内部定点倍率；新增按 `L/min` 手工设置并预览 `P/s`，保存时仍写入原始脉冲基准，后续修改流量计系数只改变换算显示。学习、手工设置和清除合并为同一行操作，未启用水路也可维护基准。设备校准参数改为真正三等分展示，修改入口独立于参数网格，弹层桌面三列、窄屏单列；三项 NVS 写入增加保存结果与读回核对，失败时恢复原值。执行 `pio test -e native` 通过 73/73 个测试用例，新增覆盖手工流量解析边界和 `L/min` 到 `P/s` 的定点往返；嵌入式脚本语法及存储字段契约检查通过，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 81068 B / 24.7%、Flash 1217853 B / 77.4%。通过 `/dev/cu.usbserial-57460296581` 两次尝试烧录均未进入写入阶段，分别因串口监视占用和设备未正确进入下载模式失败，设备原固件未被覆盖；仍需在设备进入下载模式后重新烧录，并验证页面最终视觉效果、三项参数实机 NVS 读回和真实水路手工基准报警。

2026-07-17 运行趋势、记录页面与关阀后水流恢复优化：普通浇水运行页的流量图改为按实际 5 秒样本动态显示时间范围，最多 10 分钟；横轴使用适量相对时间刻度，纵轴按数据自动选择整洁上限并四等分显示流量刻度，最新点单独标记，同时降低非核心标题、指标、水路和说明的字重。浇水记录列表移除内部编号，完成时间使用无年份短格式，补充实际出水水路、计划/实际时长和异常摘要；详情改为结果摘要、四项核心指标、执行概况和紧凑水路明细。关阀后水流仍按配置的延时、滚动窗口和报警脉冲数触发，但报警后必须连续一个完整窗口为 0 脉冲才恢复，系统参数说明同步明确该规则。执行 `pio test -e native` 通过 74/74 个测试用例，新增覆盖窗口内仍有单个脉冲时保持报警和零脉冲满窗后恢复；运行页嵌入脚本通过 Node.js 语法检查；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 81068 B / 24.7%、Flash 1222229 B / 77.7%。本轮未烧录设备；仍需由用户在实机确认运行图表和记录页最终视觉效果，并在真实管路验证关阀余流、异常发生与零脉冲恢复。

2026-07-17 水路基准保存稳定性修复：手工设置、学习结果保存和清除基准共用的配置保存链路不再在 `loopTask` 调用栈中同时创建两份约 1.2 KB 的完整配置副本，改用配置存储对象持有的复用缓冲区；公共保存调用链的静态栈占用降低约 2.4 KB，保留原有临时文件、读回校验、备份和原子替换策略。同步将三个超过 `Esp32Base` 字段说明长度限制的关阀后水流参数说明压缩到允许范围内，避免参数注册失败阻断设备启动。执行 `pio test -e native` 通过 74/74 个测试用例，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83468 B / 25.5%、Flash 1222297 B / 77.7%。通过 `/dev/cu.usbserial-57460296581` 完整烧录成功，四个写入区 Hash 校验全部通过；设备启动后完成 NTP 同步并恢复 Web 服务，主页请求正常跳转至灌溉页面。未代替用户修改现有基准配置，仍需用户在实机页面各执行一次手工保存和清除，确认操作响应与业务结果。

2026-07-17 配置备份假错误日志修复：水路基准手工设置与清除已经由用户各验证两次，均保存成功且页面响应正常，四次 POST 用时 420–574 ms。确认每次操作后的 `config.bak does not exist` 来自旧备份删除成功后再次调用 `exists()`检查不存在文件，并非保存或备份失败；现改为直接采用删除接口的明确结果，不再对已删除路径做二次存在性探测，配置写入、读回校验、备份轮换和失败恢复策略保持不变。执行 `pio test -e native` 通过 74/74 个测试用例，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83468 B / 25.5%、Flash 1222289 B / 77.7%。

2026-07-17 首页交互、运行流量与浇水记录重构：手动浇水计划模板改为最多两列直接卡片，点击即填入并在手工调整后解除选中；空闲首页按 5 秒、隐藏页面按 15 秒局部更新关阀后水流监测，只有空闲与运行布局切换时整页加载；运行图表将 `L/min` 移入标题，存在基准时绘制虚线，并区分等待水流、水流已建立、流量正常和高低流量。浇水记录 Store 升级为 v2，每路新增正常浇水阶段平均流量和本次基准监测标志，详情统一左对齐并改为实际/计划单行对照、平均流量和监测状态，脉冲降为折叠技术信息；v1 记录保留在独立历史目录，不迁移、不覆盖。执行 `pio test -e native` 通过 75/75 个测试用例，新增覆盖平均流量只统计正常浇水阶段及 v2 编解码和加权汇总；首页与运行页两个完整嵌入脚本通过 Node.js 语法检查，静态检查确认旧下拉模板、二次填入按钮和图内单位均已删除；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83524 B / 25.5%、Flash 1229253 B / 78.2%。本轮未烧录设备；仍需在实机复核 v2 `StoreStatus.capacity`、空闲报警发生/恢复局部更新、计划卡操作、基准虚线和浇水结果最终视觉效果。

2026-07-17 基准精度、流量事件与历史流量语义重构：水路基准统一提升为 `P/s × 10000`，手工输入 `L/min` 保存前执行高精度定点换算和三位小数精确回算，不增加仅用于展示的重复字段；配置结构升级为 v3，旧结构按当前方案不迁移。低/高流量首次连续确认后立即写事件，不再等待水路结束；继续浇水时连续正常达到同一确认时间后恢复当前状态，同一次运行再次偏差可以重新进入报警状态，但同水路同类型事件最多写一条。流量事件主信息改为检测流量、当时基准、偏差和处理结果，校准、基准修改及关阀后水流事件同步改为业务值，内部字段保留在折叠技术信息。浇水记录 Store 升级为 v3，每路增加当时基准与最近五个完整窗口的末段平均、范围和稳定性，详情区分整段平均与末段流量，列表显示具体异常水路；事件页增加四项持续条件总览。首页把两个计时明确命名为“任务总历时”和“有效浇水”，前者包含设备准备、等待水流和水路切换。执行 `pio test -e native` 通过 77/77 个测试用例，新增覆盖 `20.000 L/min` 精确回环、报警恢复后再次生效、末段五窗口稳定性和 v3 记录编解码；4 段完整静态脚本通过 Node.js 语法解析；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83868 B / 25.6%、Flash 1235085 B / 78.5%。本轮未烧录：设备现有 v2 灌溉配置与既有 App Events 需要在安装 v3 前按当前方案明确清理并重新配置，直接烧录会使业务保持安全停机；待用户确认维护窗口后再执行烧录、写入校验和启动日志检查。页面最终视觉效果、真实水路末段稳定判断和报警发生/恢复仍需用户实机验收。

2026-07-17 记录存储与事件状态复核修复：格式化 LittleFS 后若基础库没有成功重载浇水记录 Store，业务层会按当前 v3 定义重新创建、核验并在需要时重新登记，不再把一次重载失败永久保留为存储故障。持续状态总览改为按当前卡片状态统计，等待判断不计入异常；事件状态写入失败时仍保留已知的物理正常或异常状态。关阀后水流在首页、事件页和状态接口区分等待余流消退、收集首个完整窗口、浇水暂停、正常和异常，并提供剩余秒数。低/高流量连续正常确认恢复后重新布防，同次浇水再次确认偏差会写入新的异常段事件；新业务字段启用新的事件码，避免旧事件按新字段误解。执行 `pio test -e native` 通过 77/77，4 段嵌入式脚本语法检查通过，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83868 B / 25.6%、Flash 1236449 B / 78.6%。通过 `/dev/cu.usbserial-57460296581` 烧录成功，四个写入区 Hash 校验全部通过；软件重启日志确认业务 ready、`watering.v3` ready、容量 2254、记录与事件存储均无故障，WiFi、Web 和 NTP 正常。实机 HTTP 确认记录页恢复为 0 条记录而非存储不可用，关阀监测从首窗剩余 5 秒转为正常。为避免再次删除用户配置和事件，本轮没有重复执行破坏性的整盘格式化；格式化后的自愈分支仍需下次维护窗口做一次实机验证。DS3231 仍报告 `i2c_error`，需要在硬件侧继续排查。

2026-07-17 首页与运行状态信息降噪：空闲首页不再用“一切正常”掩盖硬件时钟降级，RTC 不可用而 NTP 正常时明确说明断网影响；正常关阀后水流只显示稳定的“监测已开启”或“监测准备中”，窗口秒数、阈值和脉冲数不再吸引日常注意，真实异常改为说明泵阀均已关闭并引导查看事件。运行页顶部压缩为单一任务名称和“当前水路 · 当前阶段”，手动任务不再重复三次“手动浇水/正在浇水”。完整格式化 LittleFS 后同步清除持续条件基线并立即重新观察，使仍存在的 RTC 等异常在新事件历史中重新形成发生事件；单独清空事件历史仍保留基线。执行 `pio test -e native` 通过 77/77；首页和运行页两段完整嵌入脚本通过 Node.js 语法检查；执行 `pio test -e esp32_record_test --without-uploading --without-testing` 通过设备事件测试固件编译；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83868 B / 25.6%、Flash 1236813 B / 78.6%。本轮未烧录设备；仍需实机执行一次 LittleFS 格式化，确认约 60 秒后重新产生“硬件时钟不可用”事件，并由用户验收桌面与窄屏首页、运行页最终视觉效果。

2026-07-17 首页关阀监测与 RTC 警告细化：首个有效关阀窗口不再显示含义模糊的“准备中”，统一显示“关阀后水流监测中”；报警时首页次要信息每 5 秒刷新实际滚动观察时长、脉冲数和按当前稳态流量系数换算的窗口平均流量，明确不把该值描述成瞬时流速。RTC 不可用但 NTP 正常时不再改变首页主状态背景或增加独立黄色面板，改为在日期与 NTP 来源下方显示“硬件时钟不可用 · 断网后计划可能暂停”紧凑标签。执行 `pio test -e native` 通过 77/77，新增覆盖关阀滚动窗口实际观察时长；首页和运行页两段完整嵌入脚本通过 Node.js 语法检查；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83868 B / 25.6%、Flash 1237785 B / 78.7%。本轮未烧录设备；仍需在实机制造关阀后水流脉冲，核对首页动态数值与实际流量计系数，并由用户验收 RTC 标签和桌面、窄屏布局。

2026-07-17 浇水失败记录与首页结果信息完善：普通浇水请求只要已经进入执行流程，无论是否建立水流，结束时都写入浇水记录；零水流失败保留失败水路、计划时长、零有效浇水和后续未执行水路。浇水异常事件新增明确的手动/自动来源及自动计划上下文，事件列表和详情结合当前配置显示计划、水路与浇水记录入口。首页不增加重复报告，继续使用“下一次自动浇水”和“最近一次浇水”两张卡片：前者突出计划、执行水路数、预计总时长和水路摘要，后者显示来源、计划、开始至完成时间、结果说明、计划与有效浇水时长及历史估算水量；运行结束后只短暂强调更新后的最近记录卡。记录列表将“实际水路”改为“执行情况”，零水流失败不再显示空白。执行 `pio test -e native` 通过 77/77，新增覆盖零脉冲失败记录；执行 `pio test -e esp32_record_test --without-uploading --without-testing` 通过设备事件测试固件编译，事件测试增加自动计划和水路标题映射；4 段完整嵌入脚本通过 Node.js 语法检查；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83868 B / 25.6%、Flash 1241733 B / 78.9%。串口 `/dev/cu.usbserial-57460296581` 已连接，但本机无法解析设备主机名，无法在烧录前确认设备空闲，本轮未烧录；仍需在确认维护窗口后完成写入校验、启动日志检查，并由用户在实机验收桌面和窄屏首页。

2026-07-18 浇水记录信息结构统一：删除记录页 CSV 入口、下载路由和导出实现，当前产品只保留分页列表与详情。列表统一为浇水任务、执行水路、执行结果、实际 / 计划和估算用水量；首页最近记录去除与状态标签重复的结果文案，并统一使用计划浇水、实际浇水和估算用水量。详情将当时基准流量独立成列，整段平均和末段流量分别紧跟相对基准差值，稳定性改为“稳定 / 未确认稳定”，高低流量结论改为“未触发 / 曾触发报警”，没有基准时只说明本次不进行高低流量判定。运行页同步将无报警状态从“流量正常”收敛为“流量监测中”。执行 `pio test -e native` 通过 77/77；4 段完整嵌入式脚本通过 Node.js 语法解析；静态检查确认 CSV 入口、路由、实现和当前方案引用均已删除；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 83868 B / 25.6%、Flash 1242393 B / 79.0%。本轮未烧录设备，仍需用户在实机验收记录列表、详情弹层和运行状态的桌面与窄屏最终样式。

2026-07-18 Flash 存储可靠性优化：保留浇水记录 512 KiB、App Events 100 KiB 和系统文件日志 4 × 32 KiB 的现有容量，只将浇水记录追加后的最低文件系统空闲要求从 96 KiB 调整为 32 KiB。低/高流量确认后先把首个检测值锁存在 RAM，水泵和阀门输出关闭后再写 App Events，同一水路同一类型每次浇水最多写一条。调度状态改为先保存 32 字节带 CRC 的 blob，再写首次初始化标记；即使两次写入间掉电，下次启动也优先恢复有效 blob，不会把已保存状态误判为缺失。浇水任务已经使用启动时复制的完整运行快照，因此运行中继续允许保存计划、水路、系统参数及自动浇水暂停/恢复状态，修改只影响后续任务，PWM 频率延迟到当前任务结束后应用。执行 `pio test -e native` 通过 78/78，新增覆盖运行中修改外部请求与配置不改变当前任务；执行 `pio test -e esp32_record_test --without-uploading --without-testing` 通过设备测试固件编译，并增加 32 KiB 阈值及“blob 已写、标记未写”恢复用例；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 84004 B / 25.6%、Flash 1242445 B / 79.0%。本轮未烧录或运行设备测试，仍需在实机验证浇水期间保存只影响后续任务、报警事件在安全停机后落盘、调度状态掉电恢复以及 LittleFS 接近 32 KiB 空闲时的轮换行为。

2026-07-18 首次烧录存储故障提示优化：保持基础库启动不自动格式化 LittleFS，也不扫描分区或增加首次启动状态；首页不再把文件系统无法挂载、已有配置无有效副本、配置写入失败和其它启动检查失败统一描述为配置不兼容。新设备文件系统不可用时明确说明可能需要初始化，并要求确认没有需保留数据后到 System 页主动格式化；已经使用过的设备明确禁止仅凭提示直接格式化。格式化成功后仍由现有回调自动创建默认配置和当前版本存储。静态检查确认四类提示、System 直达入口和旧误导文案删除；执行 `pio test -e native` 通过 78/78，执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 84004 B / 25.6%、Flash 1243361 B / 79.1%。本轮没有烧录或执行破坏性的格式化；仍需在新设备未初始化 LittleFS、已使用设备挂载失败和无效配置副本三个实机状态下核对最终提示。

2026-07-20 实际浇水反馈闭环：修复浇水结束后用较新的 `millis()` 复位关阀监测、随后又用本轮较旧时间观察所造成的无符号回绕，现有 30 秒关阀后检测延时不再被瞬间绕过。多水路执行新增可配置的先断后通间隔，默认 1000 ms、范围 100～10000 ms；上一水路完成停泵和关阀后，泵及全部阀在间隔内保持关闭，再开启下一水路，间隔余流不计入下一水路。首页在普通浇水、校准和基准学习期间继续显示秒级设备时间、日期、来源和运行状态；计划页使用明确文字、标签、边框和克制底色区分自动执行开启/关闭及总控暂停；自动计划未执行事件按手动浇水、另一自动计划、校准、学习、任务收尾、功能未就绪、计划无效和输出启动失败分别说明原因。配置结构升级为 v4，旧结构不迁移。执行 `pio test -e native` 通过 79/79，新增覆盖重力供水和泵控模式的水路切换间隔、全部输出关闭及余流隔离；7 段完整嵌入脚本通过 Node.js 语法解析；执行 `pio test -e esp32_record_test --without-uploading --without-testing` 通过设备事件测试固件编译；执行 `pio run -e esp32_irrigation` 通过，资源占用 RAM 84028 B / 25.6%、Flash 1252969 B / 79.7%。当前未检测到 ESP32 串口，且安装 v4 前需要用户确认配置维护窗口，因此没有烧录或格式化；仍需在完整 PCB 上验证真实阀门切换电流、关阀余流与 30 秒延时，并由用户验收计划页和运行页的桌面、窄屏样式。

仍需在完整 PCB 和真实水路上验证电磁阀、泵、流量计、RTC、突然断电及 LittleFS 真实写失败。本地 LCD2004 与四按钮功能按方案明确留待 Web 功能稳定后另行设计。
