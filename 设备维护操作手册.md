# 设备维护操作手册（烧录 / Web OTA）

本文是个人维护用操作手册：首次或需要整片烧录用串口；日常升级固件用 Web OTA（浏览器或命令行），不需要拆机接线。

> 重要边界
> - 我（Codex）只负责源码、构建和产物核对；**烧录、OTA、复位等设备操作由你本人执行**。
> - OTA 与烧录前先确认没有正在浇水或基准流量学习任务（设备在 OTA 期间会拒绝升级）。
> - OTA 只升级 app，**不会清除** Wi-Fi、水路、计划和配置；factory 整片烧录会重写分区表，升级前务必核对目标设备。
> - 测试设备与正式设备分开，确认 IP/主机名和网页认证账号后再操作。

---

## 0. 前置：本机构建固件

所有命令的工作目录是 **IoT 工作区根目录**（`/Users/tyg/workspace/iot`），不是 firmware 目录。

### 0.1 一键构建（推荐，自动选工具链）

```sh
python3 foundation/Esp32Base/scripts/pio_arduino.py 3 --tls-toolchain run \
  -d devices/Esp32_Irrigation/firmware \
  -e esp32_irrigation_arduino3
```

成功后关注末尾三行：

- `Total image size` / `OTA image size`：镜像字节数和对侧 OTA 槽余量（当前约 22%）。
- 产物路径：
  - OTA 用：`devices/Esp32_Irrigation/firmware/.pio/build/arduino3-tls/esp32_irrigation_arduino3/firmware.bin`
  - 串口整片烧录用：同目录 `firmware.factory.bin`（**OTA 不要用这个文件**）

### 0.2 用 PlatformIO 直接构建（命令行 OTA 需要）

```sh
foundation/Esp32Base/.piohome/arduino3-tls/penv/bin/pio run \
  -d devices/Esp32_Irrigation/firmware \
  -e esp32_irrigation_arduino3
```

> 两种方式产物一致；0.1 的封装脚本会管理 Core 3 + TLS 工具链，首次构建按它的提示走即可。

### 0.3（可选）核对产物

```sh
shasum -a 256 devices/Esp32_Irrigation/firmware/.pio/build/arduino3-tls/esp32_irrigation_arduino3/firmware.bin
ls -la devices/Esp32_Irrigation/firmware/.pio/build/arduino3-tls/esp32_irrigation_arduino3/firmware.bin
```

升级后可在设备网页页脚的 OTA/系统状态页看到当前镜像 SHA256，与此比对确认刷的是预期版本。

---

## 1. 方式一：浏览器 Web OTA（最简单，推荐）

前提：设备已联网、能打开设备网页，电脑与设备在同一局域网（同一路由器，非访客网络）。

1. 构建固件（见第 0 节）。
2. 浏览器打开设备首页 `http://<设备IP或主机名>/irrigation`，登录网页账号密码。
3. 翻到**页脚系统区**，打开 **OTA（升级）**入口（Base 内置页面，路径 `/esp32base/ota`，可直接访问）。
4. 选择文件：
   `firmware/.pio/build/arduino3-tls/esp32_irrigation_arduino3/firmware.bin`
   页面会先显示文件大小并与下一 OTA 槽容量比对，超大会直接拒绝、不会发送。
5. 点上传/升级，等待写入完成、设备自动重启（约 1 分钟内）。
6. 刷新首页确认版本/时间正常；设备启动后新版正常运行会自动确认本次升级（mark valid），若新固件启动失败会自动回滚到上一版，不会变砖。

多台实验机时，每台都用它自己当前的 IP 重复以上步骤即可，互不影响。

---

## 2. 方式二：命令行 Web OTA（`pio run -t webota`）

适合频繁升级或浏览器上传不稳定的弱网环境；复用同一套 OTA 引擎、Basic Auth、容量预检，默认 64 KB 分块并按 RSSI 自动降速。

### 2.1 配置本机目标设备（只做一次，不进 git）

配置文件为 `devices/Esp32_Irrigation/firmware/platformio.local.ini`
（该文件已在 `.gitignore`，不会提交，当前已按本机配好）。内容形如：

```ini
[env:esp32_irrigation_arduino3]
upload_speed = 460800
custom_esp32base_webota_host = 192.168.2.141
custom_esp32base_webota_user = <网页账号>
custom_esp32base_webota_password = '<网页密码>'
```

> 段名必须是 `[env:esp32_irrigation_arduino3]`（实际构建/烧录/OTA 用的环境）；
> 旧文件里的 `[env:esp32_irrigation]` 是 Arduino Core 2 时代的环境，已不生效。

- `host`：设备 IP，或 mDNS 主机名 `irrigation-controller.local`（换机器时改 IP 最直接）。
- 不想把密码写文件，可用环境变量代替（前缀 `ESP32BASE_WEBOTA_`）：

```sh
export ESP32BASE_WEBOTA_HOST=192.168.2.141
export ESP32BASE_WEBOTA_USER=<网页账号>
export ESP32BASE_WEBOTA_PASSWORD='<网页密码>'
```

> 换另一台实验机：改 `host`（和账号密码，如有不同）即可；也可以临时用环境变量覆盖。

### 2.2 执行升级

```sh
foundation/Esp32Base/.piohome/arduino3-tls/penv/bin/pio run \
  -d devices/Esp32_Irrigation/firmware \
  -e esp32_irrigation_arduino3 \
  -t webota
```

日志会依次打印：解析到的 IP、预检（认证 + 本地镜像与下一槽容量比对）、RSSI 与分块策略、上传进度、写入与重启。预检失败不会发送固件。

### 2.3 弱网时的可选参数（在 local.ini 的 env 段添加）

```ini
custom_esp32base_webota_chunk_size = 16384   ; 默认 65536，弱信号降到 16384/8192
custom_esp32base_webota_upload_timeout = 180 ; 上传超时秒数，默认 90
```

跨 VLAN、访客 WiFi、禁用组播的网络下不要依赖 `.local`，直接填 IP。

---

## 3. 串口烧录（首次烧录 / 整片恢复 / OTA 不可用时）

需要 USB 连接，并且要**先核对串口对应的是哪台设备**（不要按串口编号猜设备）。

### 3.1 普通 app 烧录（保留分区表，推荐串口方式也用这个）

构建后用 esptool 只写 app 槽：

```sh
foundation/Esp32Base/.piohome/arduino3-tls/penv/bin/esptool \
  --chip esp32 --port /dev/cu.<目标设备串口> --baud 460800 \
  write-flash 0x10000 \
  devices/Esp32_Irrigation/firmware/.pio/build/arduino3-tls/esp32_irrigation_arduino3/firmware.bin
```

### 3.2 整片工厂烧录（新板 / 分区变动 / 文件系统需要重建时）

`firmware.factory.bin` 已包含 bootloader(0x1000)、分区表(0x8000)、boot_app0(0xe000)、app(0x10000)：

```sh
foundation/Esp32Base/.piohome/arduino3-tls/penv/bin/esptool \
  --chip esp32 --port /dev/cu.<目标设备串口> --baud 460800 \
  write-flash 0x0 \
  devices/Esp32_Irrigation/firmware/.pio/build/arduino3-tls/esp32_irrigation_arduino3/firmware.factory.bin
```

也可以直接用 PlatformIO 上传（按 `platformio.ini` 已配置的分区表）：

```sh
pio run -d devices/Esp32_Irrigation/firmware -e esp32_irrigation_arduino3 \
  -t upload --upload-port /dev/cu.<目标设备串口>
```

默认 upload 写 bootloader(0x1000)、分区表(0x8000)、boot_app0(0xe000)、app0(0x10000)，
不写 nvs(Wi-Fi/认证) 和 LittleFS(0x370000 配置/记录)，所以日常这样烧不丢数据。
串口波特率由 `platformio.local.ini` 的 `upload_speed` 控制，本机设为 460800（约快 4 倍，稳定优先；
不稳就降 115200，想再快可试 921600）；
Core 3 的 esptool v5 显示为单行动态进度条（旧版是逐行百分比），属正常现象。
若出现 `Failed to connect ... No serial data received`，通常不是固件问题：换一根数据线、确认端口，
并手动让 ESP32 进入下载模式（按住 BOOT，点一下 RST 后松开 BOOT）再上传。

### 3.3 看启动日志确认

```sh
foundation/Esp32Base/.piohome/arduino3-tls/penv/bin/python -m serial.tools.miniterm \
  /dev/cu.<目标设备串口> 115200
```

正常应看到 LOCAL/app0、Wi-Fi 连接、`business_ready records_fault=no events_fault=no scheduler_fault=no`、Web ready。看完 `Ctrl-]` 退出。

---

## 4. 升级后确认与回滚

1. 刷新首页：时间走时、水路/计划/设置仍在（OTA 不清配置）。
2. 页脚 OTA/系统状态页核对镜像 SHA256 与大小；确认没有异常标签。
3. 若新固件反复重启、无法提供服务，设备会自动回滚上一版；也可在 OTA 状态页确认当前运行槽位（app0/app1）。
4. OTA 失败但旧版网页还能打开时，原样重传一次即可；raw 上传失败不会切换启动分区。

## 5. 常见问题

- **上传被拒绝 / 提示忙碌**：等当前浇水或流量学习结束后再升级。
- **401/认证失败**：用网页登录的同一组账号密码；命令行检查 local.ini 或环境变量。
- **容量预检失败**：确认选的是 `firmware.bin` 而不是 `firmware.factory.bin`，且是刚构建的当前版本。
- **`.local` 解析不了**：改用设备 IP；确认电脑与设备同一子网、mDNS 未被隔离。
- **找不到串口**：`ls /dev/cu.usbserial-*` 列出端口，结合设备拔插前后差异确认具体端口。
