# 灌溉升级与平台接入工作记录

更新：2026-09-10。此文件继续作为设备侧本轮任务记录；类型级验收仍以 [灌溉类型账本](../../platform/iot-device-lab/device-types/irrigation-controller/README.md) 为准，不另建系统台账。

## 当前目标与边界

通过最新 Esp32Base 和 iot-device-sdk 完成可试运行固件，修复明确业务缺陷，复用基础能力并对齐平台。设备负责业务与执行器安全，Base 负责通用底座，SDK 负责平台协议。零历史兼容、零数据迁移。

仅做代码评审、目标编译和快速定向测试；不执行烧录、复位、OTA、真实 MQTT 控制或泵阀动作。物理目标及授权另行核对，不能引用其他实验板许可。长期、满容量和反复断电实验由用户后续试运行验证。

## 已核实基线

初查时以下仓库均无未提交改动；下列是本地 HEAD，不表示全部已重新比对远端最新状态：

| 责任仓库 | 分支 / 提交 | 本轮相关事实 |
| --- | --- | --- |
| Esp32_Irrigation | main / 5be1248 | 已有直接 MQTT 接入，尚无 SDK 依赖 |
| Esp32Base | main / 1fbd528 | 当前基础库；存在固定 Core 3.3.8 TLS 产物 |
| iot-device-sdk | codex/record-stream / 53b4a74 | 会话、型号、命令和单流可靠记录能力 |
| iot-device-lab | main / eb1971f | 灌溉定义、协议和类型账本权威 |
| iot-home-server | main / 7811cc3 | 服务端实现，具体适配尚待审查 |
| iot-wx-apps | main / 0c21c9b | 客户端实现，具体适配尚待审查 |
| iot-base | main / 529d9d0 | 共同架构权威 |

固件 `firmware/platformio.ini` 已声明 `symlink://../../../foundation/Esp32Base`，固定 espressif32 6.7.0 / Arduino Core 2；尚未通过本轮构建确认实际解析和最终链接产物。设备已有 watering / irrigation-audit 两个独立 Store，本地历史与补发共用事实。旧记录中的“仅实施 Base、灌溉尚未接入存储”已经过时。

## 已确认问题与待决策点

1. **本地来源错误。** 本地 Web 调用 `IrrigationApp::startManualWatering/startSingleOutput`；`IrrigationIot::serializeRecord` 却把全部非自动浇水标成 `wechat_miniprogram`。当前定义 sourceKey 也只有微信与设备调度，不能准确表达本地操作。
2. **未知时间记录阻塞补发。** 本地启动不以获得可信 UTC 为前提；记录发布遇到无法解析完成时间时直接返回 false。当前 watering Schema 强制 startedAt/completedAt 为时间字符串且 timeQuality 只能是 trusted，不能如实传输未知时间事实。不得用重放时间补造发生时间或越过未确认记录。
3. **TLS 工具链不满足 SDK。** 固件启用缺失证书日期校验的例外；SDK Esp32MqttPort 明确拒绝该能力缺失。Base 已有固定 Core 3.3.8 / ESP32 完整 TLS 产物，可复用，仍须核对哈希并完成本目标链接检查。设备 BoardHardware 使用 Core 2 LEDC API，需要随目标升级调整并验证安全关闭及失败路径。
4. **设备重复平台实现。** IrrigationIot、IrrigationIotProtocol、命令 journal 和 RecordSync 仍自行承担平台职责。SDK 接入需接管共同会话、校验、命令生命周期和每流同步；两个既有业务 Store 维持独立，不能为迁就 SDK 当前单流会话绑定而合并业务格式。
5. **诊断缺口。** 类型定义已有 state.diagnostics，当前真实固件 StateBit 只有八项业务投影，尚未接上诊断心跳。

建议的最小公共契约调整（**待用户确认，未实施**）：允许本地来源；允许未知时间记录以空绝对时间和明确 unknown 质量保存、同步和展示；未知时间记录不归入按日期统计，同时明确显示统计未覆盖量。保留本地离线浇水能力，不伪造微信来源或可信时间。

## 待办与实施顺序

- [ ] 待用户决策：确认上述来源和未知时间记录的公共契约及统计处理。当前暂停相关实施，无进行中编码任务。
- [ ] Esp32_Irrigation：继续定向评审业务、安全与失败路径，修复明确 bug；切换已验证的完整 TLS 工具链并对齐 Base。尚未改源码、编译或运行本轮测试。
- [ ] iot-device-lab / SDK / 设备：按确认后的当前定义接入 SDK，消除重复协议实现、接入两条独立记录流及真实诊断；核对缓冲和资源预算。
- [ ] 服务端 / 小程序：只同步必要适配，完成来源、未知时间和灌溉命令/状态/记录的快速定向链路验证；不以模拟通过冒称实机通过。
- [ ] 每个可独立交付阶段审阅 diff、定向验证、提交及安全推送，更新本记录；交付目标固件和未验证边界。

## 本轮证据

已完成规则、配置、相关源码及定义的只读初查。设备远端 main 经 ls-remote 确认与 5be1248 一致。本轮仅更新此任务记录并检查 diff；未修改公共协议、生产源码、凭据、服务实例或硬件。旧 G4/G5 结果不得自动套用到尚未产生的新固件。
