# 存储体系重构工作记录

> 本文件用于跨会话保存本轮重构的已确认方向和阶段状态。当前先完成 `Esp32Base`，灌溉固件、IoT 协议、服务端和小程序暂不接入；全部完成后再决定是否删除本文件。

## 1. 问题

当前灌溉固件存在本地浇水历史、IoT 可靠记录流和 App Events 多份持久化数据，LittleFS 预算相互竞争，也存在重复写入。基础库虽已有定长分段 `Esp32BaseRecordStore`，但所有 LittleFS 访问尚未统一协调，FileLog 仍有直接访问 LittleFS 的位置，格式化后的恢复流程由 Web 层硬编码，多 Store 的总预算和维护边界还不完整。

## 2. 最终方向

采用三层结构：

1. **Storage Coordinator**：统一所有 LittleFS 读写的串行化、容量保留、受管路径、OTA/格式化维护状态和持久模块恢复生命周期；不解释业务数据格式，也不重复实现 LittleFS/ESP-IDF 的物理磨损均衡。
2. **独立存储引擎**：FileLog、普通文件和多个定长 RecordStore 分别维护自己的格式、CRC、轮转和故障状态，全部通过统一文件系统入口访问。
3. **业务与 IoT 适配层**：设备项目定义每种业务记录 payload；IoT 项目以后再负责每个 Store 的独立 stream、sequence 和累计 ACK。

不采用统一变长混存日志、SQLite/通用数据库、动态 Schema、后台 compaction，也不把所有数据格式合并进一个 Flash Manager。

## 3. 业务模型

最终只保留：

- `Operation`：远程操作由服务端 command/receipt/progress 形成；设备只保存无服务端命令的必要本地/自动操作结果。
- `BusinessRecord`：完整、不可变、值得查询和统计的业务事实。
- `Condition`：当前持续状态；确认发生和恢复时更新小型状态检查点，只有确实需要追溯时才追加紧凑记录。
- `TechnicalLog`：独立 FileLog。

取消独立 Event 产品级存储和页面概念。一次性事件归入 Operation 或 BusinessRecord，持续事件改为 Condition。

## 4. 多 Store 与容量

每种主要固定 payload 使用独立 RecordStore 和目录。设备通常只有 1～3 个 Store，Store 之间独立版本、容量、轮转、故障和后续同步水位。

业务记录总预算保留两档：

- Small：256 KiB；
- Large：512 KiB。

它们是设备全部业务 Store 的合计，不是每个 Store 的预算。当前灌溉 896 KiB LittleFS 下，Large 512 KiB 加 FileLog 128 KiB 后保留约 256 KiB 给 LittleFS 元数据、临时维护和安全余量，不再扩大。

每个Store按预算和实际槽位从8/16/32/64 KiB级别选择段上限，使常见32～512 KiB预算通常不超过16个完整/尾段文件；不再限制整个设备只有8个业务文件。典型结果：64 KiB用约8 KiB段、128 KiB用约16 KiB段、256/384 KiB用约32 KiB段、512 KiB用约64 KiB段。具体段数和容量以 `StoreStatus` 为准，不能只用预算整除段上限，因为段头、槽位取整和尾段会影响结果。

后续灌溉接入暂定为384 KiB浇水记录加可选128 KiB紧凑审计；当前约752 B的最坏槽位至少约可保留516条。若只使用单个512 KiB主Store，则至少约688条。实际payload是否还能缩小，留到灌溉接入阶段重新设计。

## 5. 当前实施边界

本阶段只修改并完善 `Esp32Base`：

- 建立统一 LittleFS 协调和维护生命周期；
- 移除基础库模块对 LittleFS 的旁路访问；
- 完善多个定长 RecordStore 的注册、总预算、分段、状态和维护；
- 将 Condition 当前状态能力与 App Events 历史解耦，收敛独立 App Events；
- 更新基础库文档、示例和测试，并验证 Core 2.x/3.x 与四 Profile 裁剪。

本阶段明确不做：

- 不修改 `Esp8266Base`；
- 不修改灌溉固件的数据格式和接入；
- 不修改 `iot-base` 权威协议；
- 不修改设备实验室、服务端或小程序；
- 不设计最终灌溉 payload；
- 不执行会启动泵阀的实机动作。

## 6. 本阶段完成结果

`Esp32Base` 已完成：

- `Esp32BaseFs` 对所有LittleFS操作使用递归串行化保护，提供独占维护和写暂停；
- FileLog不再直接调用LittleFS，并阻止FS错误日志递归写回FileLog；
- 新增 `Esp32BaseStorage`，统一最多8个Store登记、默认512 KiB总预算、受管路径、非受管可写容量、统一清空、格式化恢复和OTA写暂停；
- Store登记从Web移到Storage，System页、FS上传和OTA均使用同一协调层；
- RecordStore按预算和槽位自适应选择分段；
- 删除App Events历史能力、页面、API、示例和旧能力宏；新增独立 `Esp32BaseConditions`，只在NVS保存当前活动位图；
- 已通过原生存储/Condition/多Store测试、Core 2.0.16和Core 3.3.8四Profile构建。未进行灌溉接入或泵阀动作。

## 7. 后续顺序

1. 下一轮重新压缩灌溉记录payload，确定是否需要Compact Audit Store。
2. 灌溉本地历史和IoT outbox改为同一份权威Store，移除重复持久化。
3. 再制定多Store MQTT、服务端和小程序匹配方案。
4. 最终根据文档是否仍有长期价值决定保留或删除本文件。
